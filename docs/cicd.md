# CI/CD

GitHub Actions deploys to GitHub Pages on push to `master`. The workflow runs `npm ci` and `npm run build`.

See [.github/workflows/deploy.yml](../.github/workflows/deploy.yml) for the full configuration.
